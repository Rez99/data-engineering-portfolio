name: project-3-streaming-bot-detection-m1

services:
  jobmanager:
    image: apache/flink:1.19.1-scala_2.12-java17
    platform: linux/amd64
    container_name: p3-flink-jobmanager
    command: jobmanager
    ports:
      - "8081:8081"
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
        rest.address: jobmanager
        rest.bind-address: 0.0.0.0
        taskmanager.numberOfTaskSlots: 8
        parallelism.default: 2
    volumes:
      - ../../streaming:/opt/flink/jobs:ro
      - ../../batch:/opt/flink/batch:ro
      - ../../data/flink/lib:/opt/flink/usrlib:ro
      - ../../data:/opt/flink/data

  taskmanager:
    image: apache/flink:1.19.1-scala_2.12-java17
    platform: linux/amd64
    container_name: p3-flink-taskmanager
    command: taskmanager
    depends_on:
      - jobmanager
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
        taskmanager.numberOfTaskSlots: 8
        parallelism.default: 2
    volumes:
      - ../../batch:/opt/flink/batch:ro
      - ../../data/flink/lib:/opt/flink/usrlib:ro
      - ../../data:/opt/flink/data

  validation-job:
    image: apache/flink:1.19.1-scala_2.12-java17
    platform: linux/amd64
    container_name: p3-flink-validation-job
    depends_on:
      - jobmanager
      - taskmanager
      - redpanda
    command:
      - bash
      - -lc
      - |
        /opt/flink/bin/sql-client.sh \
          -l /opt/flink/usrlib \
          -D execution.target=remote \
          -D rest.address=jobmanager \
          -f /opt/flink/jobs/flink_job_validation.sql
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
        rest.address: jobmanager
    volumes:
      - ../../streaming:/opt/flink/jobs:ro
      - ../../batch:/opt/flink/batch:ro
      - ../../data/flink/lib:/opt/flink/usrlib:ro
      - ../../data:/opt/flink/data
    restart: "no"

  analytics-job:
    image: apache/flink:1.19.1-scala_2.12-java17
    platform: linux/amd64
    container_name: p3-flink-analytics-job
    depends_on:
      - jobmanager
      - taskmanager
      - redpanda
    command:
      - bash
      - -lc
      - |
        /opt/flink/bin/sql-client.sh \
          -l /opt/flink/usrlib \
          -D execution.target=remote \
          -D rest.address=jobmanager \
          -f /opt/flink/jobs/flink_job_analytics.sql
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
        rest.address: jobmanager
    volumes:
      - ../../streaming:/opt/flink/jobs:ro
      - ../../batch:/opt/flink/batch:ro
      - ../../data/flink/lib:/opt/flink/usrlib:ro
      - ../../data:/opt/flink/data
    restart: "no"

  analytics-observer-job:
    image: apache/flink:1.19.1-scala_2.12-java17
    platform: linux/amd64
    container_name: p3-flink-analytics-observer-job
    depends_on:
      - jobmanager
      - taskmanager
      - redpanda
    command:
      - bash
      - -lc
      - |
        /opt/flink/bin/sql-client.sh \
          -l /opt/flink/usrlib \
          -D execution.target=remote \
          -D rest.address=jobmanager \
          -f /opt/flink/jobs/flink_job_analytics_observer.sql
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
        rest.address: jobmanager
    volumes:
      - ../../streaming:/opt/flink/jobs:ro
      - ../../batch:/opt/flink/batch:ro
      - ../../data/flink/lib:/opt/flink/usrlib:ro
      - ../../data:/opt/flink/data
    restart: "no"

  operational-job:
    image: apache/flink:1.19.1-scala_2.12-java17
    platform: linux/amd64
    container_name: p3-flink-operational-job
    depends_on:
      - jobmanager
      - taskmanager
      - redpanda
      - postgres
    command:
      - bash
      - -lc
      - |
        JAR=/opt/flink/data/flink/generated/operational-bot-scoring.jar
        if [[ ! -f "$${JAR}" ]]; then
          echo "Missing $${JAR}. Run ./infra/scripts/infra_setup_m4.sh first." >&2
          exit 1
        fi
        /opt/flink/bin/flink run -d \
          -c com.portfolio.botdetection.OperationalBotScoringJob \
          -D rest.address=jobmanager \
          -C file:///opt/flink/usrlib/flink-sql-connector-kafka-3.2.0-1.19.jar \
          -C file:///opt/flink/usrlib/flink-connector-jdbc-3.2.0-1.19.jar \
          -C file:///opt/flink/usrlib/postgresql-42.7.3.jar \
          "$${JAR}" \
          --bot-config /opt/flink/batch/artifacts/bot_config.json \
          --normalization-values /opt/flink/data/flink/generated/normalization_values.csv \
          --parallelism 3
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
        rest.address: jobmanager
    volumes:
      - ../../streaming:/opt/flink/jobs:ro
      - ../../batch:/opt/flink/batch:ro
      - ../../data/flink/lib:/opt/flink/usrlib:ro
      - ../../data:/opt/flink/data
    restart: "no"
